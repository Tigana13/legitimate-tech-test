import { InvoiceCustomizationDirective } from './invoice-customization.directive';

describe('InvoiceCustomizationDirective', () => {
  it('should create an instance', () => {
    const directive = new InvoiceCustomizationDirective();
    expect(directive).toBeTruthy();
  });
});
