import { SampleChartDirective } from './sample-chart.directive';

describe('SampleChartDirective', () => {
  it('should create an instance', () => {
    const directive = new SampleChartDirective();
    expect(directive).toBeTruthy();
  });
});
