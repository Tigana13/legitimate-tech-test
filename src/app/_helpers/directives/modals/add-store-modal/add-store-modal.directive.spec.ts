import { AddStoreModalDirective } from './add-store-modal.directive';

describe('AddStoreModalDirective', () => {
  it('should create an instance', () => {
    const directive = new AddStoreModalDirective();
    expect(directive).toBeTruthy();
  });
});
